import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
    container: {
        marginVertical: 12,
        width: '100%',
    },
    scroll: {
        paddingHorizontal: 16,
        gap: 12,
        flexDirection: 'row',
    },
});

import { StyleSheet } from "react-native";
import { Spacing } from "../../../shared/theme/spacing";
export const styles = StyleSheet.create({
    container: {
        gap: Spacing.lg,
    },
    field: {
        gap: Spacing.xs,
    },
    shimmerBlockMarginTop: { marginTop: Spacing.xs },
    shimmerBlockMarginTop2: { marginTop: Spacing.md }
});


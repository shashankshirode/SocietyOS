import { Platform } from 'react-native';
export const HapticFeedback = {
    light: () => {
        if (Platform.OS === 'web')
            return;
        try {
        }
        catch {
        }
    },
    medium: () => {
        if (Platform.OS === 'web')
            return;
        try {
        }
        catch {
        }
    },
    success: () => {
        if (Platform.OS === 'web')
            return;
        try {
        }
        catch {
        }
    },
    warning: () => {
        if (Platform.OS === 'web')
            return;
        try {
        }
        catch {
        }
    },
    error: () => {
        if (Platform.OS === 'web')
            return;
        try {
        }
        catch {
        }
    },
};


import { dataSourceConfig } from '../dataSource/dataSource.config';
export type { DataSourceMode } from '../dataSource/dataSource.types';

export { dataSourceConfig };

export const DATA_SOURCE_MODE = dataSourceConfig.globalMode;
